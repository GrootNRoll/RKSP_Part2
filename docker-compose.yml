version: '3.8'
services:
  equipment-test:
    build: .
    ports:
      - "${HOST_PORT:-8080}:5000"
    environment:
      - PORT=5000
      - DB_PATH=/data/equipment_log.db
      - FLASK_DEBUG=${DEBUG:-false}
    volumes:
      - ./data:/data
    env_file:
      - .env.docker